# ============================================================
# c1-versions.tf
# Terraform version constraints and provider configuration.
# ============================================================

terraform {
  required_version = ">=1.10.0"
}